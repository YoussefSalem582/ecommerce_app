# ADR 002: GoRouter + Stateful Shell

**Status:** Accepted

**Decision:** `GoRouter` with `StatefulShellRoute` for tab shell, auth redirect in `AppRouter._redirect`, paths in `AppRoutes`.

**Consequences:** Deep links and web URLs work; auth guard is centralized.
